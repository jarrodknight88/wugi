rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // Allow public reads for images
    match /{allPaths=**} {
      allow read: if true;
    }

    // Only authenticated users can upload
    match /{allPaths=**} {
      allow write: if request.auth != null;
    }

  }
}