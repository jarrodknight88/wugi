{
  "name": "wugi-functions",
  "description": "Wugi Firebase Cloud Functions",
  "scripts": {
    "build": "node node_modules/typescript/bin/tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test:grammar": "tsc && node scripts/test-inbound-grammar.js",
    "test:apify-normalize": "npm run build && node scripts/test-apify-normalize.js",
    "test:event-transform": "npm run build && node scripts/test-event-transform-core.js && node scripts/test-event-transform-routing.js",
    "test:intel-media": "npm run build && node scripts/test-intel-media.js",
    "test:media-moderation": "npm run build && node scripts/test-media-moderation.js",
    "test:dispatch-queue": "npm run build && node scripts/test-dispatch-queue.js",
    "test": "npm run build && node scripts/test-apify-normalize.js && node scripts/test-inbound-grammar.js && node scripts/test-event-transform-core.js && node scripts/test-event-transform-routing.js && node scripts/test-intel-media.js && node scripts/test-media-moderation.js && node scripts/test-dispatch-queue.js"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google-cloud/vision": "^5.3.7",
    "exif-reader": "^2.0.3",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "passkit-generator": "^3.5.7",
    "resend": "^6.10.0",
    "sharp": "^0.33.5",
    "stripe": "^14.0.0",
    "twilio": "^5.13.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.9.3"
  },
  "private": true
}
