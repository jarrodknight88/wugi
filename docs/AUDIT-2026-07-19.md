# State of the Union Audit — 2026-07-19

**Scope:** Code-only archaeology of the repo against the Asana launch board
(re-baselined launch: Sep 1, feature freeze Aug 3, App Store submission ~Aug 12).
Asana ref: STATE OF THE UNION AUDIT (GID 1216689529254412).
**Method:** Read-only inspection of `origin/main` (`bed3a92`), full branch/PR
history, and grep/read across `mobile-app/`, `functions/`, `dashboard/`, `web/`,
`check-in-app/`, `lens/`. No Firestore access, no builds, no installs.
**Verdict key:** SHIPPED / PARTIAL / NOT STARTED / CANNOT DETERMINE (CD =
code is wired but only a device/runtime test can confirm behavior).

**Tally: 48 SHIPPED / 20 PARTIAL / 7 NOT STARTED / 4 CANNOT DETERMINE (79 tasks)**

---

## A. Repo inventory

### Open PRs

**None.** Every PR is closed. Notably:

- **PR #26 (deals, all surfaces) is MERGED** — 2026-06-19, squash commit
  `2bf55d9`. Not stuck; no action needed.
- **PR #23 closed unmerged** — superseded the same day by PR #24 (same fix,
  correct key path), which merged as `2b187fc`.

### Branches

All remote branches are squash-merge leftovers or defunct; **nothing unmerged
is sitting on any branch** (verified by tree-diff against main, including the
post-review `partial-redeem` commit on `awesome-wozniak`, which is in main).

| Branch | Status | Merge-readiness |
|---|---|---|
| `claude/wugi-consumer-conversion-review-n0ixz7` | PRs #32/#33/#34 merged (funnel flip + social auth, Google client ID, CLAUDE.md); tip identical to main ancestor | Safe to delete |
| `claude/series-modal-redesign` | PR #31 merged (series modal redesign) | Safe to delete |
| `claude/series-media-actions` | PR #30 merged (series cover image + archive/delete) | Safe to delete |
| `claude/dashboard-series-recurrence` | PR #29 merged (recurrence + seriesSlug writes) | Safe to delete |
| `claude/teranga-reset` | PR #28 merged (reset-to-teranga script) | Safe to delete |
| `claude/amazing-ride-g9ipz6` | PR #27 merged (series audit script + admin gate) | Safe to delete |
| `claude/deals-all-surfaces` | **PR #26 merged** (deals to Venue/Discover/For You/search) | Safe to delete |
| `claude/awesome-wozniak-wc0b8s` | PR #25 merged (unified pass rendering) incl. follow-up partial-redeem fix — verified in main | Safe to delete |
| `claude/intelligent-carson-4gm74m` | PR #23 closed unmerged; superseded by #24 | Safe to delete |
| `claude/create-user-on-login-B0Dmg` | March 2026 prototype (EventProfileTest), 61 commits, pre-baseline | Defunct; delete |

Passes/ticketing work status: the passes/ticketing branch work (#25) and deals
(#26) are **both merged**; the launch-relevant passes code all lives on main.

Notable direct-to-main commits (no PR): screen-guard self-heal series
(`7c0bc99`, `6260946`, `c7bb69e`, `127a1f5`), launch/search crash hotfix
(`23d1bc6`), UAT venue fixes (`274dbb7`, `997e029`, `8af3a4a`, `72bdb28`),
build #75/#76 series-system work (`d76c220`, `3c4a141`, `13937a5`, `f3aff39`).

---

## B. Task-by-task verdicts

### Consumer app — auth & account

| GID | Task | Verdict | Evidence & notes |
|---|---|---|---|
| 1214022932539398 | [S1-01] Signup first screen, auth gate w/ guest bypass | **PARTIAL** | Guest bypass shipped, but the auth-gate half was deliberately **inverted** by AUTH-FLOW-01 (`e9556d1`): splash→main for everyone, auth is a dismissible intent sheet (`RootNavigator.tsx:59-93,251,422-437`). Board scope no longer matches the shipped product — close/rewrite the task. |
| 1214022911126868 | [S1-02] Username required at signup + phoneIndex | **PARTIAL** | `UsernameScreen.tsx` exists but is post-signup with "Skip for now" (line 161) — not required. `phoneIndex`: `firestoreService.ts:322-343` `savePhoneNumber` + firestore.rules exist but have **zero callers** — dead code. |
| 1214055108401793 | [S1-1c] Email verification, block unverified access | **PARTIAL** | Send-on-signup shipped (`FirebaseContext.tsx:164-166`); `EmailVerifyBanner` above tabs (`RootNavigator.tsx:628`). Access is a **soft banner, not a block** — intentional, but differs from task scope. |
| 1214055372254579 | [S1-1d] Dedicated ForgotPasswordScreen | **SHIPPED** | `ForgotPasswordScreen.tsx`, wired `RootNavigator.tsx:450-452`, linked from `SignupScreen.tsx:373-380`. |
| 1214055147557123 | [S1-1e] Auth email branding / custom domain | **CD** | Transactional (Resend) mail uses `tickets@wugi.us` (`emailService.ts:14`), but Firebase **auth** email branding is console config, not code-visible. Confirm in Firebase console → Auth → Templates. |
| 1214486988611750 | [AUTH-FLOW-01] Auth lifecycle refactor | **SHIPPED** | `e9556d1` (Jul 9): phase machine, intent-based auth sheet, guest→Firestore merge, social sign-in (`RootNavigator.tsx`, `FirebaseContext.tsx`, `SignupScreen.tsx`). |
| 1214486987778307 | [BUG-51-01] Verification email never sent | **SHIPPED** | Send on signup (`FirebaseContext.tsx:164-166`); friendly Resend errors + 60s cooldown (`EmailVerifyBanner.tsx:51-87`). Delivery itself needs one runtime send test. |
| 1214113583225400 | [S1-32] Account notifications toggle dynamic/collapsible | **SHIPPED** | `AccountScreen.tsx:353-427` — per-permission states, master toggle + collapsible sub-options, OneSignal tag sync. |

### Consumer app — payments, passes, tickets

| GID | Task | Verdict | Evidence & notes |
|---|---|---|---|
| 1214022910325836 | [S1-03] Face ID saved cards end-to-end | **CD** | Fully wired: `PaymentScreen.tsx:119-147` + `createPaymentIntentHttp.ts:175-209` (customer/ephemeralKey, `setup_future_usage`). Needs device test: biometric prompt + saved-card reuse. |
| 1214019642524528 | [S1-07] Card management screen | **NOT STARTED** | No screen exists anywhere; card save is implicit via PaymentSheet at checkout only. No view/delete/set-default. |
| 1214019642499236 | [S1-08] Face ID as payment auth gate | **CD** | `LocalAuthentication.authenticateAsync` gate before PI creation (`PaymentScreen.tsx:124-147`). Device test needed. **Gate silently skipped when no biometrics enrolled** (see risks). |
| 1214113596541610 | [S1-29] Face ID gates saved card payments | **CD** | Same gate scoped to saved-card reuse (`PaymentScreen.tsx:123`). Device test needed. |
| 1214051108593176 | [S1-09] MyPassesScreen QR + detail UX | **SHIPPED** | `PassScreens.tsx:457-608` (list) + `161-449` (viewer: pulsing QR, detail bottom sheet), live listener. |
| 1214019619952888 | [S1-10] Guest checkout full flow | **SHIPPED** | `PaymentScreen.tsx:57-91`, `createPaymentIntentHttp.ts:218-224`, `webhook.ts:109-427`, guest email confirmation `PassScreen.tsx:139-193`. Guests get passes by email only (by design — Firestore rules). |
| 1214100393670321 | [S1-24] Apple Wallet pass gen, paid + free | **SHIPPED** | `generatePass.ts` buildPassBuffer; paid `webhook.ts:342-385`, free `createPaymentIntentHttp.ts:103-136`. Depends on `functions/certs/*` shipping with deploy (see risks). |
| 1214100402343005 | [S1-25] Post-purchase confirmation | **SHIPPED** | `PassScreen.tsx:139-238` (guest) + `254-425` ("You're in!", share, multi-pass pager). |
| 1214105226380173 | [S1-26] My Passes grouped by order | **SHIPPED** | `groupPassesByOrder` + `PassGroupCard` expandable rows (`passGrouping.ts`, `PassGroupCard.tsx:147-169`). |
| 1214094510009328 | [S1-27] QR viewer shows event info | **SHIPPED** | `PassScreens.tsx:306-333`. |
| 1214113582684041 | [S1-31] Transferred ticket opaque w/ details | **SHIPPED** | `PassGroupCard.tsx:53-86` (0.45 opacity, TRANSFERRED badge, non-interactive; details via sheet). |
| 1214028754260481 | [S2-07] Pass sharing | **PARTIAL** | Backend complete (`ticketTransfer.ts`: initiate/claim/cancel + tokens); in-app TransferModal + web claim page. Named `PassShareSheet`/`PassClaimScreen` don't exist; claim is **web-only** — `wugi://tickets/claim/{token}` deep link has **no RootNavigator handler**. |
| 1214115968513098 | Balance payment webhook | **SHIPPED** | `webhook.ts:66-70,430-479` (clears balanceDue, unlocks guest passes) + `web/app/api/balance-payment/route.ts`. |
| 1214115902829470 | BUG: claimed ticket QR re-access | **SHIPPED** | Claim redirects to permanent `/tickets/{passId}` (`ClaimForm.tsx:50-52`; `PassView.tsx` persistent QR). |
| 1214115846729517 | BUG: Wallet "Ticket not found" after claim | **SHIPPED** | `ticketTransfer.ts:211-231` generates + stores new `.pkpass`, sets `appleWalletPassUrl`; proxy `web/app/api/wallet/[passId]/route.ts`. Residual: wallet **live-update** pushes 404 for claimed passes (keyed by orderId — see risks); initial download works. |
| 1214107552503655 | [UAT-4] Free RSVP already-claimed error | **NOT STARTED** | `createPaymentIntentHttp.ts:66-152` free path has **no dedup/idempotency** — double-RSVP creates duplicate passes and double-decrements inventory. No client guard either. |
| 1214105214978862 | [S1-23] Dashboard: ticket type status field | **SHIPPED** | `dashboard/app/dashboard/events/page.tsx:259-262` (on_sale/sold_out/cancelled), enforced server-side (`createPaymentIntentHttp.ts:41`). Inconsistency: per-event editor `[eventId]/page.tsx` TicketModal still uses an `active` boolean. |
| 1214115874108430 | Dashboard: tableCapacity field | **SHIPPED** | `events/page.tsx:290-305`, consumed `createPaymentIntentHttp.ts:48-51` + `webhook.ts:246-288`. Missing from `[eventId]` TicketModal. |
| 1214115846646906 | Web pass page matching app style | **SHIPPED** | `web/app/tickets/[orderId]/page.tsx` + `PassView.tsx` mirrors app viewer (colored bg, pulsing QR, Add to Wallet). Route param named `[orderId]` but resolves passId. |

### Consumer app — notifications & analytics

| GID | Task | Verdict | Evidence & notes |
|---|---|---|---|
| 1214100383739373 | [S1-19] Push prompt on first launch | **SHIPPED** | `useNotifications.ts:33-37` (requestPermission) invoked at root `App.tsx:47`. Native prompt render = device test. |
| 1214093973811388 | [S1-20] In-app notifications scope | **PARTIAL** | Foreground display + tap routing only (`useNotifications.ts:86-118`). No OneSignal IAM, no notification inbox/center. |
| 1214093974644273 | [S1-21] OneSignal Data Tags ↔ toggles | **SHIPPED** | `AccountScreen.tsx:401-419` (addTags for events/deals/galleries + optIn). Server side only targets 'All' segment so far — tags not yet consumed (see risks). |
| 1214100370490874 | [S1-18] Email + SMS trigger audit | **PARTIAL** | Wired: purchase email/SMS (`webhook.ts:391,414`), transfer + reclaim email, door receipts + balance-paid SMS (`terminalFunctions.ts:299-338`). **Dead:** `sendCheckInSMS` / `sendTicketScannedSMS` (`smsService.ts:103,118`) have zero callers. No balance-paid email. |
| 1214105210631943 | [S1-22] Firebase Analytics conversion events | **NOT STARTED** | No analytics SDK in `mobile-app/package.json`; zero `logEvent` calls; `docs/launch-checklist.md:211` unchecked. Auto-collection via plist only; no custom events anywhere. |

### Consumer app — feeds, venue, UX

| GID | Task | Verdict | Evidence & notes |
|---|---|---|---|
| 1214487945036546 | [VENUE-DATA-04] Query layer + integrity | **SHIPPED** | `firestoreService.ts` getApproved* + `notTestVenue()` (L374) + `safeData.ts` guards + `23d1bc6` undefined-field hotfix. |
| 1214488434011943 | [VENUE-DATA-07] Pagination + prefetch + caching | **SHIPPED** | Paged queries (`firestoreService.ts:591,631`), prefetch (`App.tsx:66-79`), `catalogStore.ts`, expo-image memory-disk. **Caveat:** React-Query hooks + catalogStore are unwired — no screen imports them (dead path, see risks). |
| 1214492726394289 | [VENUE-DATA-08] Image bug + crash instr. + dedupe | **SHIPPED** | `crashLogger.ts` + `ErrorBoundary` on EventScreen (`EventScreen.tsx:810-827`), `computeSeriesFeed` dedupe (`firestoreService.ts:414`), expo-image source normalization. |
| 1214487004828866 | [HOME-FEED-01] Home feed refactor | **SHIPPED** | Time-aware hero + 3 shelves, real data only (`HomeScreen.tsx:148,406-413,650-683`). |
| 1214488416086225 | [FORYOU-FEED-01] For You refactor | **SHIPPED** | Swipe deck + `getForYouFeed()` + deals interleave, honest states (`ForYouScreen.tsx:192-258`). |
| 1214485841290019 | [VENUE-SCREEN-01] Accordion-with-preview | **SHIPPED** | v2 sectional rebuild (`20b9355`); `VenueContactBlock` inline expand. |
| 1215717566821277 | Venue upcoming: collapse by series | **SHIPPED** | `274dbb7` + `997e029` + `72bdb28`; `computeSeriesFeed` in VenueScreen, View-all list, inline cap 8. |
| 1215695526238403 | Cross-page series dedup | **PARTIAL** | Dedup applied **within** each page only; cross-page explicitly deferred in code comment (`firestoreService.ts:635-640`). |
| 1215695415759095 | Build #77 screen-guard re-integration | **SHIPPED** | `127a1f5`; native `modules/secure-image-view`, `PhotoViewer.tsx:34,300` with fallback warn. |
| 1215699330689229 | Screen-guard blackout post-screenshot | **SHIPPED** | Heal ladder `SecureImageView.swift:157-195` (`7c0bc99`,`6260946`,`c7bb69e`). Effectiveness needs device test; heavier deterministic fallback documented but intentionally unwired. |
| 1215695526277670 | Per-night gallery archive on event page | **PARTIAL** | Series gallery surfaces on occurrences (`useEventGalleriesBySeriesId`, `EventScreen.tsx:580-628`), but event page shows newest only; multi-night archive lives on the venue page, not the event page. |
| 1214022932541134 | [S2-08] Onboarding walkthrough (4 slides) | **PARTIAL** | First-launch pager shipped (`OnboardingScreen.tsx`, `RootNavigator.tsx:77,398-405`) but **3 slides, not 4**. |
| 1214019642499243 | [S2-09] Skeletons replace spinners | **PARTIAL** | Gallery shimmer only (`GalleryScreen.tsx:48`); ActivityIndicator still in 16 screens incl. Home/ForYou. |
| 1214028775894285 | [S2-10] expo-image + blurhash | **PARTIAL** | expo-image adopted app-wide with memory-disk caching; **blurhash: zero matches** — never built. |
| 1214026536324100 | [S2-22] All screens live Firestore | **SHIPPED** | All feeds/screens on live queries; favorites hydrate on login (`RootNavigator.tsx:300-354`). Residual: `makeGallery` mock stub still attached to card `.gallery` fields (see risks). |
| 1215311498421114 | [UAT V4] Skewed gallery thumbnails | **SHIPPED** | Masonry layout (`a411f63`, `GalleryScreen.tsx:37`). |
| 1215311602647019 | [UAT V4] Search overlay load time | **SHIPPED** | `016cbf9`; search-data keep-alive + cached taxonomy (`DiscoverEditorialScreen.tsx:876-917`). Perceived speed = device check. |
| 1215311498785946 | [UAT V4] Masonry + pagination at 50 | **SHIPPED** | Cursor pagination, page size 50, onEndReached (`GalleryScreen.tsx` header L13-27). |
| 1215470404140837 | [UAT V6] galleryDocToData venue field | **SHIPPED** | `1daae7b`; real venue name resolved at call sites (`DiscoverEditorialScreen.tsx:158-166`). |
| 1215471690400964 | [UAT V6] Menu sticky header z-index | **SHIPPED** | `1daae7b`; `MenuScreen.tsx:47-49,520` status-bar inset. |
| 1215310718163254 | [UAT V4] Liking photo → Saved | **SHIPPED** | `PhotoViewer.tsx:146` → favorites type 'photo'; `FavoritesScreen.tsx:353,481` Saved Photos section; hydration `RootNavigator.tsx:352`. (Stale in-file comment predates fix.) |
| 1215310805623094 | [UAT V4] Vibe + Amenities from Firebase | **SHIPPED** | `filters/{vibes,amenities}` fetched at runtime (`DiscoverEditorialScreen.tsx:36-100`); hardcoded lists are fallback-only. |

### Wugi Door

| GID | Task | Verdict | Evidence & notes |
|---|---|---|---|
| 1214108517367331 / 1214094253129966 | [DOOR-05/09] Standalone build, no Metro | **SHIPPED** | `check-in-app/eas.json` (Release, no dev client), `app.config.js`, `scripts/build-testflight.sh`, CLAUDE.md build guide. Metro-free launch itself = build/device check. |
| 1214108517539485 | [DOOR-06] Free RSVP in check-in list | **SHIPPED** | Free passes `source:'free'` (`createPaymentIntentHttp.ts:66-101`); list reads `source!='door'` (`ManualLookupScreen.tsx:300-317`). |
| 1214094510005615 | [DOOR-07] Paid tickets in list (QR mismatch) | **PARTIAL** | List + in-app QR (passId) work. **The named QR/passes mismatch is unresolved for Apple Wallet**: wallet QR encodes orderId (`generatePass.ts:133`, `webhook.ts:350`) while scanner looks up `passes/{passId}` (`ScannerScreen.tsx:74-81`) → wallet passes scan Invalid. |
| 1214108516820782 | [DOOR-08] Free ticket passes in list | **SHIPPED** | `createPaymentIntentHttp.ts:71-133`; same wallet-QR caveat as DOOR-07. |
| 1214020524844314 | [S1-14] Walk-up ticket flow | **SHIPPED** | AddGuestModal → PaymentScreen walkin → `captureTerminalPayment` (`terminalFunctions.ts:192-207`). Live tap = device. Note: walk-ups write `events/tickets`, not a passes doc → not re-scannable in passes list. |
| 1214020524869133 | [S1-15] Apple Wallet color sync | **PARTIAL** | Triggers exist for `events/tickets.color` + `venues/tableColors` (`ticketColorSync.ts:12-49`, `passWebService.ts:194`), but Door writes `passes.passColor` (`ManualLookupScreen.tsx:374`) which **no trigger watches** — UI claims immediate wallet update; it doesn't happen. |
| 1214051108588771 | [S1-16] Multi super admin PIN | **SHIPPED** | `validateSuperAdminPin.ts:10-56` — multiple named secrets, constant-time compare, per-admin logging. |
| 1214051108597900 | [S1-17] GPS geofence auth | **SHIPPED** | 200m haversine (`useLocationCheck.ts`), gates event PIN (`PINScreen.tsx:95-96`); super-admin bypass by design. `__DEV__` bypass exists (see risks). |
| 1214026549522655 | [S2-01] Inactivity timeout + Face ID unlock | **NOT STARTED** | No LocalAuthentication dep; zero FaceID/idle-timeout hits in check-in-app. |
| 1214019633847892 | [S2-02] Hybrid staff identity | **PARTIAL** | PIN identity complete; **SMS magic-link half absent** (smsService only sends receipts). |
| 1214020538681310 | [S2-03] Role-based permissions | **PARTIAL** | `StaffRole` type + super_admin adaptive UI exist; `'manager'` role never gates anything; `generateDoorPin` always issues role 'door'. |
| 1214020538687723 | [S2-04] Staff audit log | **PARTIAL** | Per-action attribution everywhere (scannedBy, staffUid on payments/refunds/voids), but no dedicated queryable audit-log collection or in-app view. |
| 1214019633852882 | [S2-05] Stripe refunds (Manager+, server-gated) | **PARTIAL** | `refundDoorSale`/`cancelDoorSale` + UI shipped (`terminalFunctions.ts:370-494`, `TransactionsScreen.tsx:101-130`), but server checks only `context.auth` — **role gate unmet** (any anonymous-auth client can refund). |
| 1214022924107857 | [S2-06] Partial payment support | **PARTIAL** | Editable amount incl. balance mode, but `captureTerminalPayment` **zeroes balanceDue regardless of amount paid** (`terminalFunctions.ts:185-189`) — no remaining-balance ledger. |
| 1214115874154198 | Block guest scan when balanceDue > 0 | **SHIPPED** | `ScannerScreen.tsx:108-135` → 'balance_blocked', "do not admit" UI (:244-251). |

### Wugi Lens

| GID | Task | Verdict | Evidence & notes |
|---|---|---|---|
| 1214020524844887 | [S2-12] Photographer onboarding + profile | **PARTIAL** | Pre-provisioned email/pw sign-in only (`lens/src/screens/LoginScreen.tsx`, `App.tsx:56-59`). No signup, no onboarding, no profile create/edit; `photographers` collection defined in schema.md but unused in code. |
| 1214020524869833 | [S2-13] Photo upload + tagging | **SHIPPED** | Upload queue (`useUploadQueue.ts`), `uploadPhoto` Storage+Firestore txn (`lens/src/lib/firebase.ts:68-111`), event/venue tagging via `getOrCreateGallery` (:36-65). Caveats: tagging is gallery-scoped; watermark + quality settings saved but **not applied** in upload; `thumbUrl = url` (no real thumbnails). |
| 1215469885076574 | Incomplete gallery publishing gate | **NOT STARTED** | Galleries `status:'live'` at creation (:50-57); photos `approved:true` on write (:100); no draft/min-photo gate anywhere. Consumer-side empty-strip hiding is a display filter, not a gate. |
| 1215695493753427 | Publish gallery to specific occurrence | **SHIPPED** | Gallery keyed by occurrence eventId (:44,51); consumer prefers occurrence link over series fallback (`EventScreen.tsx:160-164`, `useEventGallery.ts:24-27`). `events/{id}.galleryId` back-link never written (admin write skipped). |
| 1216689526214260 | Lens Phase 1 (ingest, pending pool, device registry, claim page) | **NOT STARTED** | **None of the four pieces exist in code.** No functions ingest module (client uploads direct to Storage); no `pending*` collection (photos auto-approved); no device registry (router IP in AsyncStorage only; `walletDevices` is unrelated); no Lens claim page (`web/app/tickets/claim` is ticket transfer; `web/app/gallery/[galleryId]` is read-only display). Current state = pre-Phase-1 direct-upload MVP + GL.iNet router auto-sync (`useRouterSync.ts`). |

### Infra (code-visible only)

| GID | Task | Verdict | Evidence & notes |
|---|---|---|---|
| 1215695415714878 | generateSeriesEvents admin gate | **SHIPPED** | `functions/src/series/generateSeriesEvents.ts:89-102,232` (SERIES_WRITE_ROLES + assertSeriesWriteAccess), commit `a331adc` / PR #27. dryRun + scheduled job intentionally ungated. |
| 1215695493813042 | functions/lib gitignored + untracked | **SHIPPED** | `functions/.gitignore` has `lib/`; `git ls-files functions/lib` = 0; artifacts removed in `a331adc`. |
| 1215699327896321 | Min-version gate + Remote Config | **NOT STARTED** | Zero hits for remoteConfig/forceUpdate/minVersion across the app; `crashLogger.ts:71-82` only reports version as telemetry. No forced-update kill-switch. |
| 1214487008573105 | [INFRA-VENUE-11] Schema enum reconciliation | **SHIPPED** | `firestore-v2.ts:94-105` canonical VenueStatus/EventStatus; transform (`03-transform-and-write.js:408-455,672`) and app queries ('approved') agree; `docs/status-models.md` matches for venues. Doc gap: no Event-status section in status-models.md. |
| 1214020547116555 | [S3-06] Secrets sweep | **PARTIAL** | Repo clean of server-side secrets: no tracked `.env`/`.p8`/`.pem`/serviceAccount (`git ls-files` clean); server secrets env-based. No dedicated sweep artifact exists. Findings (paths only): `mobile-app/App.tsx:44` hardcoded Stripe **live publishable** key (client-safe class, but not env-driven); 3× tracked `GoogleService-Info.plist` (mobile-app/lens/check-in-app — Firebase client config, client-safe). No secret-class material found. |

---

## C. Untracked work (no corresponding Asana task)

Substantial code shipped with no board coverage:

1. **Dispute/chargeback pipeline** — `webhook.ts:499-683` (create/update/close, venue billing, Tier-5 demotion) + `chargebacks` collection.
2. **Payout/reserve engine** — `schedulePayouts.ts`, `releaseReserves.ts`, reserve calc, `payouts` writes in webhook.
3. **Door authorize-then-capture model** — manual-capture PI + Review screen + void/refund on ID fail + `autoSettlePendingDoorSales` scheduled safety net (`terminalFunctions.ts:60-128,452-608`).
4. **ID verification pipeline (Door)** — `IDScanScreen.tsx` (AAMVA PDF417 parse, name/age/expiry match) + `aamvaParser.ts` + per-venue thresholds.
5. **Booking fee (12%) + Stripe Connect T+1 transfers on door sales** (`terminalFunctions.ts:175-242`); Door TransactionsScreen (history/revenue/refunds).
6. **Social sign-in (Apple + Google)** — `FirebaseContext.tsx:177-245`, nonce-hardened; plus `onUserCreated` server-side user-doc creation (`functions/src/users/onUserCreated.ts`).
7. **Live Wallet pass updates + APNs** — `passWebService.ts` (walletDevices registry, push), `ticketColorSync.ts`.
8. **Event-published auto-push** — `functions/src/notifications/onEventPublished.ts` (Firestore trigger → OneSignal 'All'); OneSignal↔UID linking (`useNotifications.ts:41-56`).
9. **Lens router auto-sync** — GL.iNet Slate AX polling ingest (`lens/src/hooks/useRouterSync.ts`).
10. **Web checkout parallel path** — `web/app/api/checkout/route.ts` + `createCheckoutSession.ts` (Stripe Checkout alongside mobile PaymentIntent path).
11. **ItineraryDetailScreen** curated multi-stop routes (`a0da5ff`); photo share + sharp watermark pipeline (`05e4971`, `433a308`).
12. **Ops tooling** — `scripts/audit-series.js`, `reset-to-teranga.js`, seed/backfill/scrape scripts, `build-testflight.sh`, `deploy-rules.sh`. Note: **no CI** — `.github/` has no workflows.

## D. Risk flags (Aug 3 freeze / Aug 12 submission)

### Submission blockers (verify before Aug 12)

1. **Test bundle identifier in shipping config** — `mobile-app/app.json:17,103`: iOS bundleIdentifier AND Android package are `com.wugimedia.wugitest`; Apple Pay merchant is `merchant.com.wugimedia.wugitest` (`app.json:63`, `App.tsx:131`). If the App Store record (ascAppId 6760943066) is bound to this ID, changing is a project decision — but a "-test" ID + test merchant going to the store needs an explicit go/no-go.
2. **No min-version / forced-update gate** (task 1215699327896321 NOT STARTED) — no kill-switch once builds are in the wild.
3. **Apple Wallet QR mismatch** — wallet passes encode orderId; Door scanner resolves passId → **every wallet pass scans Invalid at the door** (`generatePass.ts:133` / `webhook.ts:350` vs `ScannerScreen.tsx:74-81`). Door-critical for Labor Day.
4. **Pass-generation cert dependency** — `generatePass.ts:18,152-183` reads `functions/certs/*.pem` at runtime; failures are caught + logged silently. Verify certs ship with the deployed function.
5. **Firestore composite index** (status,isFeatured,createdAt) flagged "DO NOT auto-deploy" (`firestoreService.ts:445`) — if absent in prod, feed queries throw and return empty.

### Security / money-path

6. **Door refunds/voids not role-gated** — `terminalFunctions.ts:376,458` check only `context.auth` (anonymous auth suffices) — any client can refund/void door sales.
7. **Partial payments silently clear full balance** — `terminalFunctions.ts:185-189` zeroes balanceDue regardless of amount captured.
8. **Lens has no moderation** — photos `approved:true` on write; `eventGalleries` create allowed for any authed user (`firestore.rules:181`). Content/abuse exposure.
9. **Face ID gate bypass** — `PaymentScreen.tsx:127`: saved-card charge proceeds with no auth when biometrics aren't enrolled.
10. **Free-RSVP idempotency missing** — duplicate passes + inventory double-decrement on repeat claims (`createPaymentIntentHttp.ts:66-152`).

### Dead code / config debt

11. **Unwired React-Query catalog layer** — `useCatalogQueries.ts` + `catalogStore.ts` + `App.tsx:66-79` prefetch feed a cache no screen reads.
12. **Dead SMS senders** — `sendCheckInSMS` / `sendTicketScannedSMS` (`smsService.ts:103,118`), zero callers.
13. **Unwired phoneIndex** — `savePhoneNumber` + rules with no caller (`firestoreService.ts:322-343`).
14. **Mock remnants** — `makeGallery` stub (fake ids gp1-gp4) still attached to card `.gallery` fields (HomeScreen:82, VenueScreen:409, DiscoverScreen:52); "Menus" filter chip renders but yields zero results (`DiscoverEditorialScreen.tsx:69,705,856`).
15. **Hardcoded live Stripe publishable key** — `App.tsx:44` (client-safe class but blocks rotation); hardcoded OneSignal App ID (`useNotifications.ts:16`).
16. **`__DEV__` geofence bypass** in Door (`useLocationCheck.ts:37-40`) — confirm off in release builds.
17. **OneSignal category tags unconsumed** — server only targets 'All' segment (`sendPushNotification.ts:56-59`); Account toggles don't filter delivery until segments configured.
18. **Wallet live-update gap for claimed passes** — `passWebService.ts:58-63` keyed by orderId; claimed passes have neither doc → update pushes 404.
19. **In-app claim deep links dead-end** — web emits `wugi://tickets/claim/...` and `wugi://tickets/{id}` with no RootNavigator handler.
20. **Screen-guard blackout fix unverified on device** — heavier deterministic fallback documented but intentionally unwired (`SecureImageView.swift:188-194`).

### Board hygiene

21. Verdicts above reclassify a large share of "open" tasks as shipped — close them. Two tasks describe **inverted or superseded scope** (S1-01 auth gate → funnel flip; S1-02 required username → skippable): rewrite rather than close.
22. `lens/` landed as one squashed, mislabeled commit (`54fda18` "fix(ForYouScreen)...") — no incremental history for Lens provenance.
23. Biggest scope-vs-calendar gaps for Aug 3: **S1-22 analytics (greenfield)**, **Lens Phase 1 (all four components unbuilt)**, **S1-07 card management (not started)**, Door S2 items (all partial).
