{
  "name": "wugi-rules-test",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore security-rules emulator test suite (favorites/reports + sanity). See README.md.",
  "scripts": {
    "test": "bash run.sh"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.13.0"
  }
}
