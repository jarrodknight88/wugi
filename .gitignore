# macOS
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment (covers .env, .env.local, .env.production, .env.*.local, etc.)
.env
.env.*

# Node
node_modules/

# Build output
dist/
build/
.next/
.expo/
.expo-shared/

# Firebase
.firebase/
serviceAccount.json

# Misc
coverage/
push.sh
coverage/serviceAccount.json
service-account.json
AuthKey_*.p8

# Venue backup dumps from upsert-flagged-venues.js (large, regenerable)
scripts/backups/

# Raw API capture (scripts/capture-raw.js) — large, regenerable from API credits
data/raw/

# local run logs
logs/
.vercel
